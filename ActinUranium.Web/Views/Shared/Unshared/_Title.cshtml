@{
    var title = "Actin Uranium";
    var viewTitle = ViewData["Title"] as LocalizedHtmlString;
    if (viewTitle != null)
    {
        title = $"{viewTitle.Value} | {title}";
    }
}

<title>@title</title>